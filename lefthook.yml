pre-commit:
  parallel: false
  commands:
    format:
      glob: "*.{ts,tsx,js,jsx,json,jsonc,md,mdx,css,html}"
      run: bun run oxfmt --write {staged_files} && git add {staged_files}
    lint:
      run: bun run lint
    typecheck:
      run: bun run typecheck
    test:
      run: bun run test
